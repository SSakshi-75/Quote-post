{
  "name": "quote-post-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "server-install": "cd server && npm install"
  }
}
